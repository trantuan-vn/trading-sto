pub mod connection;
pub mod user_repo;

// Placeholder module for database access.
// Currently, the app uses in-memory storage (HashMap in AppState).
// In the future, this can be expanded to use a real DB like Postgres.